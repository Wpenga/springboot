server:
  ip: 101.35.79.108
  port: 8090                     #后端端口
  frontendAddress: "101.35.79.108:8088"  #前端地址

spring:
  application:
    name: 防控管理系统
  #热部署
  devtools:
    restart:
      #开启
      enabled: true
      #重启目录
      additional-paths: src/main/java
      # 排除静态目录
      exclude: static/**
  # 连接数据库
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mydb?serverTimezone=Asia/Shanghai
    #    GMT%2b8
    username: wpeng
    password: 'wpeng'
  #缓存数据库
  redis:
    host: localhost
    port: 6379
  mvc:
    pathmatch:
      #swagger配置
      matching-strategy: ANT_PATH_MATCHER
  #    type: com.alibaba.druid.pool.DruidDataSource
  #文件大小
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
mybatis:
  mapper-locations: classpath:mapper/*xml #扫描所有mapper下的xml文件
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

#文件存储路径
files:
  upload:
    path: /usr/server/疫情防控后端
